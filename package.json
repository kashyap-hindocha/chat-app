{
  "name": "chat-app-backend",
  "version": "1.0.0",
  "description": "MERN-chat-app",
  "main": "index.js",
  "scripts": {
    "start": "node server/index.js",
    "server": "nodemon server/index.js",
    "build": "npm install --legacy-peer-deps && npm install --legacy-peer-deps --prefix public && npm run build --prefix public"
  },
  "author": "Kashyap hindocha",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.2",
    "mongoose": "^6.2.1",
    "nodemon": "^2.0.15",
    "socket.io": "^4.4.1"
  }
}
